// import { addDoc } from "firebase/firestore";
// import { db } from "../../config/firebase";

// const collectionName = "recipes";

// // CREATE
// export const addComment = async (currentUserId, comment) => {

//     // addDoc - Cloud Firestore auto-generate an ID 
//     const resultData = await addDoc(collection(db, collectionName), comment);

//     console.log("Document written with ID: ", resultData.id);

//     return resultData;
// };
